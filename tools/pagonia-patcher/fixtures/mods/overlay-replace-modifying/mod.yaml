patchFormatVersion: "0.1"
id: pagonia-land.test.overlay-replace-modifying
name: Overlay Replace Modifying Test
version: "0.1.0"
author: Pagonia Land
gameDatabaseVersion: "1.3.1-11826+193733"
description: >-
  Artificial Pattern B overlay that Replaces the base Sawmill and changes an
  existing value (the building name). This is a genuine rewrite, not additive,
  so the base-aware advisor must NOT suggest Incremental. Tested against
  fixtures/game-gdb-mini.
requiredPackages:
  - core
entries:
  add:
    - path: overlay-replace-modifying-test/gdb/buildings.gd.xml
      source: entries/buildings.gd.xml
pak:
  name: overlay-replace-modifying-test
  summary: Advisor fixture — modifying Replace (not additive)
  author: Pagonia Land
  dependencies:
    - core
