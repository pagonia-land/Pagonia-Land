patchFormatVersion: "0.1"
id: pagonia-land.test.overlay-replace
name: Overlay Replace Test
version: "0.1.0"
author: TheLavaBlock
gameDatabaseVersion: "1.3.1-11826+193733"
description: >-
  Artificial Pattern B overlay that Replaces one inherited entity and
  references nothing else. Exercises the advisor's destructive-mode notice
  and risk score without tripping the unload-dangling warning.
requiredPackages:
  - core
entries:
  add:
    - path: overlay-replace-test/gdb/sawmill.gd.xml
      source: entries/sawmill.gd.xml
pak:
  name: overlay-replace-test
  summary: Advisor fixture — single Replace
  author: TheLavaBlock
  dependencies:
    - core
