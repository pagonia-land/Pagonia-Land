collectionFormatVersion: 0.1
id: pagonia-land.example.qol-bundle
name: Sandbox Example Bundle
version: 0.1.0
author: Pagonia Land
gameDatabaseVersion: "1.4.0-11944+194631"
description: >-
  Bundles the two example mods so the patcher applies them together. Used by
  docs/first-mods.md to demonstrate how collections work. Copy this file into
  sandbox/collections/ and point sandbox-apply.ps1 at it with -Collection.
conflictPolicy: strict

mods:
  - id: pagonia-land.example.lower-sawmill-cost
    version: "0.1.0"
    source: local
    required: true
    enabled: true
  - id: pagonia-land.example.remove-stone-cost
    version: "0.1.0"
    source: local
    required: true
    enabled: true

loadOrder:
  - pagonia-land.example.lower-sawmill-cost
  - pagonia-land.example.remove-stone-cost
